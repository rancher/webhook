name: Release

on:
  push:
    tags:
      - v*

jobs:
  build:
    name: build and package
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      run: |
          sleep 15
          true
