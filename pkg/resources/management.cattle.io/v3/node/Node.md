### Validation Checks

A node in the `local` namespace cannot be deleted as this could corrupt the
Rancher installation and potentially render it useless to spin up/down the
new/existing downstream clusters.
